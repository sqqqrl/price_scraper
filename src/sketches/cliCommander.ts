// #!/usr/bin/env node
// import { Command } from 'commander';
// import { main } from '../main';

// const program = new Command();

// program
//   .command('scrap')
//   .argument('<suitePath>', 'Path to suite that need to scrap')
//   .option('-h, --headless', 'Headless mode for puppeteer')
//   .action(async (suitePath, options) => {
//     await main({
//       suitePath,
//       options,
//     });
//   });
// // .command('sitemap');
// //   .requiredOption(
// //     '-s, --suite <path>',
// //     'The path to the suite is not provided.'
// //   )
// //   .option('-h, --headless', 'Set headless mode');

// // program.requiredOption(
// //   '-s, --sitemap',
// //   'The path to the sitemap is not provided.'
// // );

// program.parse(process.argv);

// export default program;
